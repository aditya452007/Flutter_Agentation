import 'package:flutter/foundation.dart';

/// V2 stub — temporary visual overrides (move/resize/color etc.).
///
/// V1 intentionally leaves this empty. Future levels will extend the model
/// rather than replace it. See `architecture.md:67-82`.
@immutable
abstract final class VisualChanges {
  const VisualChanges();
}
